# Postgres (docker compose up -d)
DATABASE_URL=postgres://caderneta:caderneta@localhost:5433/caderneta
# Só em produção: conexão direta (sem pooler) usada pelas migrações.
# A integração da Neon na Vercel já cria esta variável.
# DATABASE_URL_UNPOOLED=
# Opcional: resumo do mês por IA
ANTHROPIC_API_KEY=
